Углуби анализ только для ТОП-10 самых слабых страниц из предыдущего ответа.

Для каждой страницы дай:
1. `content gaps`
2. `missing subtopics`
3. `missing trust / E-E-A-T signals`
4. Что надо `переписать`
5. Что надо `расширить`
6. Можно ли страницу `оставить indexable` или лучше `noindex`

Для каждой страницы предложи:
- новый skeleton страницы
- 3–5 конкретных блоков или фраз, которые стоит добавить
- короткий вердикт: `rewrite / expand / noindex`

Сначала углуби `P1`-страницы, затем `P2`.
