# Критический SEO‑аудит (2026‑03‑19)

## Итог
**Go/No‑Go:** **GO после прохождения** `npm run predeploy:seo` и проверки прод‑окружения.

## Что сделано (правки)
- Актуализированы schema.org: `Service` для money‑страниц и `HowTo` для гайдов.
- Добавлены schema.org: `Service` для money‑страниц и `HowTo` для гайдов.
- Исправлен SEO‑футер статей (убран жесткий текст про «как выбрать кухню»).
- Приведены метаданные статей: добавлен `seoReady`, `geoTarget`, усилена внутренняя перелинковка.
- Исправлены «голые» ссылки в статье про стоимость кухни.
- Обновлен список готовых статей для индексации.

## P0 (блокеры индексации)
- **Риск: отсутствие `PUBLIC_SITE_URL`.**  
  Если переменная не задана, canonical/sitemap остаются относительными. Проверить перед релизом.

## P1 (важные улучшения)
- **Неверный SEO‑футер в статьях** (текст не совпадал с темой).  
  Исправлено на нейтральный, но релевантный текст.
- **Неполная готовность контента**: в 5 статьях не было `seoReady`, в 1 — `geoTarget`, в 2 — минимум внутренних ссылок.  
  Исправлено.

## P2 (рекомендации)
- **Structured data**: не хватало `Service` на money‑страницах и `HowTo` в гайдах.  
  Добавлено.
- **NAP‑данные**: убедиться, что `PUBLIC_BUSINESS_*` заполнены реальными данными бизнеса и一致ны с Яндекс.Бизнес.

## Чек‑лист перед включением индексации
1. Убедиться, что `PUBLIC_SITE_URL` корректен.
2. Прогнать `npm run predeploy:seo`.
3. Проверить отсутствие критических ошибок в `npm run check:schema` и `npm run check:geo-signals`.
